<h1>Edit Role Permission</h1>
<title>Role Permission: <%= controller.action_name %></title>
<%= stylesheet_link_tag 'scaffold', 'realty' %>

<% form_for(@role_permission) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :role_id %><br />
    <!--%= f.text_field :role_id %-->
    <%= f.select(:role_id, Role.all.collect{|t| [t.role_desc, t.id]}, {:include_blank => true}) %>
  </p>
  <p>
    <%= f.label :permission_id %><br />
    <!--%= f.text_field :permission_id %-->
    <%= f.select(:permission_id, Permission.all.collect{|t| [t.permission_desc, t.id]}, {:include_blank => true}) %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @role_permission %> |
<%= link_to 'Back', role_permissions_path %>
<br /><br />
<b><%= link_to 'Return to homepage...', properties_path %></b>