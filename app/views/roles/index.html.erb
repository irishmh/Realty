<h1>Listing roles</h1>

<table>
  <tr>
    <th>Role type</th>
    <th>Role desc</th>
    <th>Created userid</th>
    <th>Last updt userid</th>
  </tr>

<% @roles.each do |role| %>
  <tr>
    <td><%=h role.role_type_id %></td>
    <td><%=h role.role_desc %></td>
    <td><%=h role.created_userid %></td>
    <td><%=h role.last_updt_userid %></td>
    <td><%= link_to 'Show', role %></td>
    <td><%= link_to 'Edit', edit_role_path(role) %></td>
    <td><%= link_to 'Destroy', role, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New role', new_role_path %>